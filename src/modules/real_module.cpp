#include "real_module.hpp"

#include "imgui.h"

void RealModule::update()
{
	//TODO: résoudre le pb qui fait que l'on peut pas utiliser Imgui ici
	// Imgui::Begin("My module window");


	// if (Imgui::Button("Clique ici !")) {
	// 	// Heavy computation
	// 	std::thread t([]() {
	// 		std::this_thread::sleep_for(std::chrono::seconds(5));
	// 		Imgui::ShowPopup("Popup", "C'est fini !");
	// 	});
	// }

	// Imgui::End();
}
